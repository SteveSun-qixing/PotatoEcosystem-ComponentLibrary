/**
 * Popover 气泡卡片组件导出
 */

import Popover from './Popover.vue';

export { Popover };
export default Popover;

export type {
  PopoverProps,
  PopoverEmits,
  PopoverSlots,
  PopoverInstance,
  PopoverPlacement,
  PopoverTrigger,
} from './types';
